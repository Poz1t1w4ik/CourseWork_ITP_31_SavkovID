@model IEnumerable<StreetLighting.Models.ViewModels.LuminareViewModel>

@{ ViewData["Title"] = "Светильники"; }

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div>
                <form asp-controller="Luminare" asp-action="Index" method="post">
                    <p>
                        Тип лампы: <input type="text" name="search" />
                        <input type="submit" value="Поиск" class="btn bg-warning" />
                    </p>
                </form>
                <div>
                    <a asp-controller="Luminare" asp-action="Create" class="btn bg-warning">
                        <span>Добавить светильник</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-hover">
            <thead class="bg-warning text-black">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.LampType)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Type)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody class="table-bordered border-dark">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.LampType)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Type)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Luminare" asp-route-id="@item.Id" class="btn bg-dark p-1 text-white">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Luminare" asp-route-id="@item.Id" class="btn bg-dark p-1 text-white">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>